---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vicegerent-agent.name" . }}-config
  namespace: agent-sandbox
data:
  config.yaml: |-
    {{- include "vicegerent-agent.renderedConfig" . | nindent 4 }}
