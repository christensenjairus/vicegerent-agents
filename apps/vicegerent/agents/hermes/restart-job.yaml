---
apiVersion: batch/v1
kind: Job
metadata:
  name: hermes-restart
  namespace: agent-sandbox
  annotations:
    kustomize.toolkit.fluxcd.io/force: Enabled
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hermes-restart
    spec:
      serviceAccountName: hermes-restart
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: restart
          image: alpine/k8s:1.36.2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          env:
            - name: HOME
              value: /tmp
            - name: SANDBOX_NAME
              value: hermes
            - name: NAMESPACE
              value: agent-sandbox
          command:
            - /bin/bash
            - -c
            - |-
              set -euo pipefail
              echo "restarting sandbox ${SANDBOX_NAME} in ${NAMESPACE}"
              kubectl delete pod "${SANDBOX_NAME}" \
                --namespace "${NAMESPACE}" \
                --ignore-not-found \
                --wait=false
          volumeMounts:
            - name: hermes-sandbox-spec
              mountPath: /reload/sandbox-spec
              readOnly: true
            - name: hermes-config
              mountPath: /reload/config
              readOnly: true
            - name: hermes-soul
              mountPath: /reload/soul
              readOnly: true
            - name: codex-config
              mountPath: /reload/codex-config
              readOnly: true
            - name: claude-config
              mountPath: /reload/claude-config
              readOnly: true
            - name: hermes-approval-policy
              mountPath: /reload/approval-policy
              readOnly: true
            - name: hermes-agentgateway-api-key
              mountPath: /reload/api-key
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: hermes-sandbox-spec
          configMap:
            name: hermes-sandbox-spec
        - name: hermes-config
          configMap:
            name: hermes-config
        - name: hermes-soul
          configMap:
            name: hermes-soul
        - name: codex-config
          configMap:
            name: codex-config
        - name: claude-config
          configMap:
            name: claude-config
        - name: hermes-approval-policy
          configMap:
            name: hermes-approval-policy
        - name: hermes-agentgateway-api-key
          secret:
            secretName: hermes-agentgateway-api-key
            optional: true
        - name: tmp
          emptyDir: {}
