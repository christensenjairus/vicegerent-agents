---
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: hermes-observability-secrets
  namespace: agent-sandbox
spec:
  # Sources init-project-public-key and init-project-secret-key from the
  # Langfuse 1Password item. These are mounted only into the Hermes sandbox
  # pod — NOT shared with agentgateway-system (which uses Runtime).
  itemPath: vaults/Vicegerent/items/Langfuse
