---
apiVersion: v1
kind: Service
metadata:
  name: langfuse-otel-collector
  namespace: langfuse
  labels:
    app.kubernetes.io/name: langfuse-otel-collector
spec:
  selector:
    app.kubernetes.io/name: langfuse-otel-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: otlp-grpc
