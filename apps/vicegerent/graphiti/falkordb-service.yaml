---
apiVersion: v1
kind: Service
metadata:
  name: falkordb
  namespace: graphiti
  labels:
    app.kubernetes.io/name: falkordb
spec:
  selector:
    app.kubernetes.io/name: falkordb
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
