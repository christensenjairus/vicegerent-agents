---
apiVersion: v1
kind: Service
metadata:
  name: graphiti-mcp
  namespace: graphiti
  labels:
    app.kubernetes.io/name: graphiti-mcp
spec:
  selector:
    app.kubernetes.io/name: graphiti-mcp
  ports:
    - name: mcp
      port: 8000
      targetPort: mcp
      protocol: TCP
