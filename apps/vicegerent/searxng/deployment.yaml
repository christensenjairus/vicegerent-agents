---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: searxng
  namespace: searxng
  labels:
    app.kubernetes.io/name: searxng
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: searxng
  template:
    metadata:
      labels:
        app.kubernetes.io/name: searxng
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsUser: 977
        runAsGroup: 977
        runAsNonRoot: true
        fsGroup: 977
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: searxng
          image: docker.io/searxng/searxng:2026.6.24-e3126b89e
          env:
            - name: SEARXNG_SECRET
              valueFrom:
                secretKeyRef:
                  name: searxng-secret
                  key: secret_key
                  optional: false
            - name: https_proxy
              value: http://egress-proxy.egress-proxy.svc.cluster.local:8080
            - name: http_proxy
              value: http://egress-proxy.egress-proxy.svc.cluster.local:8080
            - name: HTTPS_PROXY
              value: http://egress-proxy.egress-proxy.svc.cluster.local:8080
            - name: HTTP_PROXY
              value: http://egress-proxy.egress-proxy.svc.cluster.local:8080
            - name: no_proxy
              value: .cluster.local,.svc,127.0.0.1,localhost
            - name: NO_PROXY
              value: .cluster.local,.svc,127.0.0.1,localhost
            - name: SSL_CERT_FILE
              value: /etc/egress-proxy-ca/ca.crt
            - name: REQUESTS_CA_BUNDLE
              value: /etc/egress-proxy-ca/ca.crt
          ports:
            - containerPort: 8080
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: settings
              mountPath: /etc/searxng/settings.yml
              subPath: settings.yml
              readOnly: true
            - name: egress-proxy-ca-cert
              mountPath: /etc/egress-proxy-ca
              readOnly: true
            - name: data
              mountPath: /var/cache/searxng
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: settings
          configMap:
            name: searxng-settings
        - name: egress-proxy-ca-cert
          secret:
            secretName: egress-proxy-ca-cert  # pragma: allowlist secret
            optional: false
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
