---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: agentgateway-ingress-from-agent-sandbox
  namespace: agentgateway-system
spec:
  endpointSelector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway-proxy
  ingress:
    # Agent sandbox → agentgateway, direct (no egress-proxy hop).
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: agent-sandbox
      toPorts:
        - ports:
            - port: '80'
              protocol: TCP
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: cerbos
            app.kubernetes.io/name: mcp-cerbos-shim
      toPorts:
        - ports:
            - port: '80'
              protocol: TCP
