---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: egress-proxy
resources: [namespace.yaml, release.yaml]
configMapGenerator:
  - name: egress-proxy-values
    files: [values.yaml=values.yaml]
configurations: [kustomizeconfig.yaml]
